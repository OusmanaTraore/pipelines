---
- hosts: server_test
  become: yes
  tasks:
  - name: run the devops container
    docker_container:
      name: devopsapp
      image: jpgibart/projet_cd_test:3.0
      state: started
      ports:
       - "8080:8081"
      command: java -jar /bin/devops-1.0.war
