---
- hosts: server_prod
  become: yes
  tasks:
  - name: run the devops container
    docker_container:
      name: devopsapp
      image: jpgibart/projet_cd:1.0
      state: started
      ports:
       - "8080:8080"
      command: java -jar /bin/devops-1.0.war